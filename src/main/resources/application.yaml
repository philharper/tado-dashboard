spring:
  application:
    name: tado-dashboard

tado:
  api:

    oauth_url: https://login.tado.com
    base-url: https://my.tado.com/api/v2
    client-id: ${TADO_CLIENT_ID}


server:
  port: ${PORT:8081}

